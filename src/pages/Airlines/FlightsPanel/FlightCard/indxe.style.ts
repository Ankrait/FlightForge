import styled from '@emotion/styled';

export const FlightPanelWrapper = styled.div`
  display: flex;
  flex-wrap: wrap;
  gap: 20px; // Здесь можно регулировать отступы между карточками
  width: 100%;
  justify-content: center; // Центрирует карточки, если место есть
`;